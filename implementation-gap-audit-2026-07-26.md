# 观象重构实施差距复盘（2026-07-26）

## 结论

当前版本没有完成原定的“完整体验顺序重构”。它完成了部分页面结构、AI 辨识后端、结果章节和观事簿基础能力，但用户真正感受到的节奏仍然接近“一张很长的表单 + 一张很长的报告”。

按层次判断：

- 数据、安全边界和确定性排盘隔离：基本成立。
- 页面章节与文案骨架：部分成立。
- 连续、逐步、沉浸的用户体验：尚未成立。
- 选定首页视觉的忠实实现：未成立。
- 每一层卦象分别结合用户现实的解读：未成立。
- HTML 离线报告的完整视觉模板：未成立。

因此，当前版本不应再被描述为“重构完成，只等细节审定”。更准确的状态是：**底层与局部组件已经铺好，但核心体验仍处在第一轮实现阶段。**

## 一、十阶段主线验收

| 阶段 | 状态 | 已做到 | 主要差距 |
| --- | --- | --- | --- |
| 1. 入境 | 实现方向错误 | 删除了旧跳转按钮和首页功能说明；放大了“观象”；保留山水、题字和向下提示 | 没有忠实采用已选第 2 张构图；仍使用旧 `hero-landscape.png` 与长卷素材叠加；顶部导航持续占据首屏；题字墨滴并非参考图效果；没有古琴控制；移动端没有等价的题字交互 |
| 2. 明法 | 半完成 | 有“观象之法”、三段经典文字、三分钟说明、四步说明和三个呼吸文案 | “我已准备好”只是锚点跳转，没有记录用户准备状态；三段文字主要依赖 hover，键盘和触屏没有等价状态；仍像说明页，不像一次真正的状态切换 |
| 3. 正问 | 基础完成 | 独立大区块、单一问题框、四个例句均已存在 | 不是受控的独立步骤；后面的辨识、成卦、观卦同时出现在同一表单；视觉上拉长了，交互上没有真正分步 |
| 4. 辨识 | 后端较完整，前台半完成 | 有真实 AI 路由、一次一问、结构化字段、事实约束、失败降级 | 没有“跳过”和“直接进入下一步”；AI 版没有直接修改建议问题的入口；完成后没有让用户看见一份清楚的事实/未知总结；当前最多 8 轮，仍可能产生被审问感；本轮无法完成线上端到端验证 |
| 5. 定问 | 半完成 | 用户可采用 AI 建议或保留原题 | 缺少“修改后采用”；确认后没有独立、明确的定问完成状态；没有把最终题目作为下一阶段仪式的视觉中心 |
| 6. 成卦 | 未达到目标 | 有三个呼吸提示、三个数字、确定性规则与 AI 隔离 | 三个呼吸提示和三个输入同时出现；三个数字仍是普通三列表单；没有逐数出现、逐数确认、墨迹收拢、三数汇聚成卦；没有单独的成卦前引导屏 |
| 7. 见卦 | 基本完成 | 结果首屏只先显示卦象、卦名、卦序和卦辞，没有先放核心判断 | 仍有较多旁注文案和入口，视觉冲击未做到“整屏只有此卦”；未用完整交互截图复核实际首屏 |
| 8. 读卦 | 结构存在，核心内容未完成 | 页面顺序包含本卦、互卦、变卦、动爻、体用与旺衰 | 本卦/互卦/变卦仍在同一三栏中一次出现，不是逐层带读；每一卦没有独立的“结合你这件事”字段；动爻、体用、旺衰的 `current_effect` 主要是通用模板，不是结合辨识材料的个性化解释；关键解释藏在默认收起的 details 中 |
| 9. 收束 | 半完成 | 核心判断被移到后部，存在最终判断、下一步和转向条件 | “下一步、何时转向”已在前一个个性化章节提前出现；有个性化结果时，“当下有利/尤其注意”反而被隐藏，没有形成要求的统一最终总结；结尾缺少完整的“不代表必然结果、仍需自行判断”的专门收束屏 |
| 10. 留存 | 基础功能完成 | 观事簿已有独立 `/journal`；可保存、打开、更新、删除；可导出单次 HTML | HTML 中嵌入字体是空数据，未真正内嵌字体；没有嵌入现有山水与印章资产；视觉只是一份简化排版；缺少默认隐藏私人问题的隐私分享版；观事簿批量导出仍为 JSON |

## 二、原七个模块的具体差距

### 1. 首页

这是当前偏差最大的部分。

1. 选定的视觉参考文件没有进入实际页面资产或构图流程。页面继续调用旧的 `hero-landscape.png`、`longscroll-landscape-v1.png` 和 `ink-golden-landscape.png`。
2. 题字后的所谓墨滴效果不是墨滴素材，而是旧山水图片被裁切、灰度化并以 0.07 的默认透明度放在文字后方。
3. 桌面悬停时透明度只提高到 0.25；`.hero-classic` 本身不可聚焦，因此 CSS 中的 `focus-within` 基本没有可操作焦点来源。
4. 手机没有 hover，也没有滚动触发或轻触触发的替代方案，所以核心效果在手机上几乎不会发生。
5. 参考方案强调大面积留白和安静的重心；当前页面保留粘性导航并叠加两层山水，视觉更满、更像原站的延伸，而不是已选方案。
6. 没有“闻琴／静音”控制。

此前 `design-qa.md` 把这些差异判为 P2“有意的产品适配”，并给出静态首页通过结论。这个判断不成立：用户已经明确选择了视觉基准，偏离不能再被解释成无需修正的适配。

### 2. 观象之法

- 三段经典文字已放大，但交互只写了 `h2:hover span` 和 `span:hover`。
- `h2` 与三个视觉 span 不可通过键盘获得焦点；span 还是 `aria-hidden`。
- “我已准备好”是普通 `<a href="#inquiry">`，没有改变页面状态，所以本质上仍是之前被要求删除的“点击后只是滚动”。
- 三个呼吸没有计时、确认或过渡，不构成真正的准备仪式。

### 3. 正问

- 四个例句和问题输入已达到原要求。
- 但“独立一屏”只是用 `min-height: 94vh` 做视觉拉长；所有步骤仍在同一个 `<form>` 中一次渲染。
- 用户可以在没有完成辨识时直接看到并操作三个数字，只是在最终提交时才收到统一错误。

### 4. 辨识与定问

后端是本轮完成度最高的部分：

- AI 不接触起卦数字，不调用确定性引擎。
- 每次最多问一个问题。
- 结构化输出包括领域、目标、范围、阶段、未知、事实、行动和回应。
- 事实类字段经过用户原文包含校验。
- 用户必须点击采用建议或保留原题。

但用户体验仍缺：

- 跳过当前问题；
- 直接结束辨识；
- 在 AI 建议基础上编辑；
- 一张让用户确认“事实 / 未知 / 已行动 / 已回应”的摘要；
- 明确显示当前是第几轮、预计还剩多少；
- 确认完成后的独立定问屏。

### 5. 成卦

目前只是把旧数字表单做了更大的排版和呼吸动画，没有实现交互逻辑重构。

- 三个呼吸同时显示。
- 三个数字同时显示。
- 数字之间没有确认动作。
- 没有“第一数结束后第二数才出现”的状态机。
- 没有三数汇聚成卦的过渡。

这只能算视觉修饰，不能算成卦仪式完成。

### 6. 结果阅读

见卦首屏的顺序已经改对，但后续读卦仍是旧的信息组织方式换了标题：

- 本卦、互卦、变卦同屏三栏；
- 经典原文和通用注释有了；
- 每卦结合现实的独立解释没有数据字段；
- 个性化解释仍是全局 `core_judgment / explanation / reality_application / action / switch_condition`；
- 动爻和体用的现实解释来自通用文化模板，而不是本次辨识信息；
- 关键章节默认折叠，用户可能直接跳到最终判断。

因此最重要的“带着用户完整解一遍卦”还没有真正完成。

### 7. 观事簿与 HTML

- 独立路由、保存、打开、更新、删除已完成。
- 单次 HTML 下载按钮已完成。
- 但 HTML 里的 `@font-face` 使用空的 base64 数据；实际会退回系统楷体。
- HTML 没有内嵌站内山水、印章和纸张素材，不是与网站一脉相承的完整离线长卷。
- 没有专门的隐私分享版。

## 三、额外发现的系统性问题

### 1. 辅助页面仍在传播旧逻辑

`/guide` 仍写着：

> 先看核心判断、眼下可做的一步与三个观察信号。需要理解依据时，再展开本卦、互卦、变卦……

这与新版要求“先见卦、逐层读卦、最后才给综合判断”正好相反。说明改版只改了主页面，没有完成产品全站口径迁移。

### 2. 视觉加法替代了交互状态机

多个需求被实现成了 CSS：

- “独立一屏”变成 `min-height`；
- “三个呼吸”变成三个同时动画的卡片；
- “逐数取数”变成三列输入；
- “墨滴洇染”变成低透明度背景图 hover；
- “我已准备好”变成锚点滚动。

这正是当前看起来“有变化”，但体验仍离目标很远的根本原因。

### 3. 上一次验收标准过低

此前主要验证了：代码编译、测试通过、首页静态截图没有崩坏。它们只能证明实现没有明显技术故障，不能证明产品体验达成。

尤其是以下路径没有完成真实验收：

- 首页题字 hover / touch / reduced-motion；
- “我已准备好”是否形成状态变化；
- AI 四至六轮完整对话；
- AI 建议后的修改与确认；
- 三数逐步输入；
- 结果从见卦到最终收束的完整阅读；
- 保存、重开、HTML 离线打开；
- 手机端完整流程。

## 四、产品边界验收

| 边界 | 状态 | 说明 |
| --- | --- | --- |
| AI 不参与确定性排盘 | 已守住 | AI 模块不接收数字，也不调用排盘引擎 |
| AI 建议问题必须确认 | 已守住 | 采用建议与保留原题由用户点击 |
| 用户陈述不冒充验证事实 | 基本守住 | 有边界文案和原文包含校验，但前台字段仍称 `confirmed_facts`，需要继续保持说明 |
| 卦象证据与现实材料分开 | 基本守住 | 页面章节分开，模型提示也有边界；但个性化段落仍需继续审查证据引用方式 |
| 不保证结果、不神秘化 | 基本守住 | 文案整体克制 |
| 音乐、动效、语音、分享可关闭或跳过 | 未完成 | reduced-motion 已有；音乐、语音、隐私分享未实现；AI 对话没有跳过 |
| 规则变化必须升级规范并测试 | 已守住 | 本轮未改变确定性规则，完整测试通过 |

## 五、重新实施顺序

### 阶段 A：先把首页与明法做对

1. 以用户选定的第 2 张视觉为唯一构图基准，不再把旧首页密度解释为“保留品牌”。
2. 制作独立的题字墨滴资产与交互动效，而不是裁切旧山水图。
3. 重新决定首屏导航呈现：首屏应先入境，导航可在下滑后显现。
4. 为墨滴、经典三句提供滚动、触屏、键盘与 reduced-motion 状态。
5. 把“我已准备好”改为真实状态确认，确认后才进入正问。
6. 加入默认静音的闻琴控制，但不阻塞流程。

### 阶段 B：把四步改成真正的状态机

1. 正问、辨识、定问、成卦按状态依次出现，不再同时渲染为一张长表单。
2. 辨识增加跳过、提前结束、编辑建议题目和结构化摘要确认。
3. 定问成为独立过渡屏。
4. 成卦增加独立呼吸屏、逐数确认与三数汇聚过渡。

### 阶段 C：升级结果数据结构

为以下内容增加独立字段与校验：

- 本卦结合本次现实的解释；
- 互卦结合本次现实的解释；
- 变卦结合本次现实的解释；
- 动爻结合本次现实的解释；
- 体用旺衰结合本次现实的限制与可借条件；
- 最终统一的可借之力、当慎之处、下一步和转向条件。

然后前端按逐层阅读顺序呈现，不再把三卦并排一次给完。

### 阶段 D：补齐留存与全站一致性

1. 重做带真实内嵌字体和图像的离线 HTML。
2. 增加默认隐藏私人信息的分享版。
3. 更新 `/guide`、`/about`、隐私说明和示例，使全站遵循同一阅读顺序。
4. 完成桌面、移动端、键盘、触屏、reduced-motion 和完整回归验收。

## 六、证据限制

本次尝试在当前运行中重新捕获完整线上流程，但：

- 私有站点在控制浏览器中停在 “Continue with ChatGPT” 登录墙；
- 本地预览被浏览器以 `ERR_BLOCKED_BY_CLIENT` 阻止。

阻断截图保存在 `.artifacts/audit-2026-07-26/`。因此本报告不是一次完整的线上截图审计；视觉参考来自用户已经选定的两张方案，实施状态来自当前源代码、数据契约、样式和测试。没有把无法看到的线上交互冒充为已验证。

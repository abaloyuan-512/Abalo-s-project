# 观象第二阶段 Beta 交付基线冻结（2026-09-06）

状态：`PHASE2_BETA_BASELINE_FROZEN`。冻结开发与交付基线，不代表全网络可达性保证、商业生产稳定性认证或最新补丁已完成所有真机验收。

## 授权与阶段口径

产品负责人在当前任务中明确：大陆直连版的验收属于第二阶段；第三阶段真正要解决解卦系统优化，包括文本产出的方向与内容。随后要求“对当前进度做再一次的核验和确认，之后就冻结”。本文件落实该授权。

- 第二阶段：网页版、VPN 手机端、大陆直连免费 Beta 的产品可用性、兼容、访问、交付及当前速度/错误恢复改进。
- 第三阶段：解卦系统与文本价值优化，解决字数过多、难懂、套话、缺少实际问题关联等问题，开展有证据的用户视角评估。
- 本口径替代旧交接材料里把大陆直连单独列为第三阶段的表述。历史文件保留，不抹掉当时的阻塞和验证记录。

## 锁定基线

| 对象 | 冻结版本与来源 | 本次核验 |
| --- | --- | --- |
| 网页版 | Sites V72；源提交 `257f00a705391192bc36beb9f2027eda001f0da1`；既有标签 `guanxiang-p1-p9-v72-frozen-20260817` | Sites 当前为 active、最新版本仍 72、原地址及私有访问策略保持；当前仓库 `sites/hosted-app` 与既有冻结标签无差异 |
| VPN 手机端 | Sites V11；源提交 `f5f6d8cf2eb2f076e9cb43237643b94b31594db3`；环境修订 4 | 官方部署状态 succeeded；线上首页/脚本 HTTP 200；原任务恢复及首屏兜底在实发脚本中存在；源仓库工作区干净 |
| 大陆直连免费 Beta | Render `guanxiang-mobile-beta`；本次既有发布来源 `8e1e2dafd843b74cf21eca34949a979eae01f00f` | 用户此前部署成功截图为该提交；本轮首页/脚本/healthz/service-wakeup 均 HTTP 200；实发脚本包含恢复和首屏兜底。公共前端健康接口不返回提交号，因此不把健康结果当作完整后端代码/环境证明 |
| 共享解卦后端 | Render `abalo-owner-preview-engine`；既有提交 `8e1e2dafd843b74cf21eca34949a979eae01f00f` | 本轮 healthz HTTP 200，回报 `git_commit=8e1e2dafd843` 及 `direct_reading_profiles=[concise-medium-v1]` |

地址：

- 网页版：https://guanxiang-owner-private-preview.abaloyuan.chatgpt.site
- VPN 手机端：https://guanxiang-mobile.abaloyuan.chatgpt.site
- 大陆直连免费 Beta：https://guanxiang-mobile-beta.onrender.com

VPN V11 部署 ID：`appgdep_6a9d43f44e188191a928cdb27d3288a4`。完整发布证据见 `docs/handoffs/2026-09-06-vpn-mobile-baseline-v11.md`。

## 配置与产品一致性

- 两个手机渠道已同步共用兼容修复、输入体验、进度提示、断线后查询原任务及现有提速配置；Sites 和 Render 的身份、持久层与部署适配分别保留。
- 本轮再次读取 VPN 环境确认 `GUANXIANG_ENGINE_PREFLIGHT=true`、`GUANXIANG_READING_PROFILE=concise-medium-v1`，修订为 4。
- 大陆配置依据此前用户设置与部署截图记录；本轮没有读取 Render 私有环境值、改配置或重新部署。已知后端支持不等于本次重新确认了 Render 全部私有设置。
- 共享后端的提速路径由手机服务端显式选择，旧请求保留默认路径；不把共享后端具备新能力写成网页版解冻。

## 核验与证据边界

1. 本轮重新运行 VPN 52 项 Node 回归/契约测试及 33 项 TypeScript 测试，85/85 通过；无输出类型检查通过。
2. 测试中的模型阶段使用固定输出，不产生付费生成。生产构建与媒体检查已在 V11 发布时通过；本轮源码未改，不重建/重发。
3. 本轮实发主脚本为大陆 `GuanxiangApp-DzuzEwln.js`、VPN `GuanxiangApp-C6IHsObE.js`，均 HTTP 200；不同打包产物名称不代表要求抹平托管差异。
4. 大陆已有产品负责人华为、同事小米可直连且使用流畅的反馈；苹果首屏修复后有一位苹果用户完整体验反馈。属于既有真实使用样本，不是全机型/全运营商统计成功率。
5. 最新速度/恢复补丁及 VPN V11 的完整真机解卦复核，按用户节约成本的决定留待下次真实问题。本轮无付费模型调用，无新增真机证据。

## 已知限制与保留待办

- Render 免费实例可能休眠，首次访问或后端唤醒仍可能等待；不承诺秒开或固定生成时长。
- 模型生成与校验仍可能失败；错误恢复补丁不等于所有上游错误、内容校验误拦均被消除。
- 当前速度小样本不是性能上限证明、p95 或 SLA；不能宣称“已到绝对最优”。
- 免费测试部署不等于正式持久存储、跨域历史数据迁移、多实例容量和运营保障完成。
- 待用户下次自然使用时复核：P6 是否正常进入 P7、P8 等待与错误恢复、数字输入无遮挡，记录设备/浏览器/网络。不得为了关闭待办主动消费模型额度。

## 冻结执行与变更纪律

- 停止第二阶段的主动功能、视觉、配置和性能迭代，当前网址继续供用户使用；保留历史版本与回退能力。
- 第三阶段在独立候选分支/版本开展内容评估与改进，不自动发布到冻结渠道；确定性规则、排盘算法及现有网页版视觉冻结继续有效。
- 影响使用的缺陷单独立项，经授权修复、回归、发布新补丁，并记录更新后的基线；不得静默覆盖旧标签或声称未复测的结果已验收。
- 本轮只写治理文档及工程入口规则，不更改产品代码、密钥、平台访问策略、部署或收费资源。
- 记录与冻结标签保存在本地仓库；不推送触发潜在自动部署，也不声称平台已设置不可变锁。未来发布仍须遵循上述授权门。

## 历史状态更正

`2026-09-05-mainland-portable-preparation.md` 的“尚未创建公网服务”和 `2026-09-06-mainland-reading-speed-v1.md` 的“等待部署/不更新 VPN”是当时状态，已由后续用户部署截图、线上健康证据及 VPN V11 发布记录推进。本文件为当前总状态入口；两份已有未提交改动原样保留，不混入本次冻结提交。

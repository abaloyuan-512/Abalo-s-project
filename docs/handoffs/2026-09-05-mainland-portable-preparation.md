# 大陆直连迁移准备与本地验证记录

记录日期：2026-09-05。目标：保持观象 0.5 产品基线，准备可脱离 chatgpt.site 运行的入口，由产品负责人最后完成大陆无 VPN 真机验收。

## 状态结论

独立运行适配与本地自动测试完成。**新公网服务尚未创建，不能宣布大陆直连成功，也不能交付一个假定可用的新网址。** 目前阻塞在部署控制台访问，而非缺少测试手机。

旧 Sites 地址与线上 Python 服务本次均未覆盖、删除或重新部署；未购买资源，未获取或提交新的密钥，未迁移线上用户数据。本地改动尚未提交/推送 Git，续接时保留全部工作区改动。

## 已完成

1. `sites/mobile-hosted-app/portable/`：Node.js 24 独立入口、本地 SQLite 持久存储适配、事务及限流兼容、服务健康检查、可配置公开地址。
2. 默认 Sites 构建保留，另加 `build:portable` 与 `dist-portable`；可独立启动，不需要浏览器直接连接 chatgpt.site。
3. 清除外来 owner/Cloudflare/代理身份头，防止独立主机误信原托管平台专用身份头；只信任显式配置的代理节点。
4. 修复 vinext 0.0.50 在 Windows 对嵌套静态文件使用本地路径分隔符、导致脚本和样式 404 的适配问题。增加精确静态文件白名单、视频分段响应、隐藏文件和源码映射拒绝返回。
5. 修复 Python HTTP 服务拒绝无密钥 POST 时未读取已发送请求体、使本机返回连接中断而非稳定 401 的问题。拒绝路径只做有大小/总时限限制的丢弃，不解析请求、不调用引擎。增加慢请求和异常长度测试。
6. 补齐全项目类型检查命令与运行环境声明生成。已有三处数据/浏览器计时器类型问题做非行为修正；未变更文案、布局、素材、动效参数或排盘算法。
7. 部署参数、持久存储要求、代理限制、回退方式及真机验收清单见 `sites/mobile-hosted-app/portable/README.md`。

## 验证结果

| 检查 | 结果及证据边界 |
| --- | --- |
| 独立构建 | `pnpm run build:portable` 成功 |
| 独立集成测试 | 6/6 通过；实际 HTTP、SQLite、Python 排盘传输；所有构建 JS/CSS 可取回；视频分段；隔离与限流；进程重启后记录、最终任务和额度仍保留 |
| 默认 Sites 回归 | `pnpm test`：50 + 13 = 63/63 通过 |
| Python 全量 | `pytest -q`：1477/1477 通过（初次 1473/1474 后发现并修复拒绝请求连接问题，新增 3 项测试） |
| 独立类型检查 | `pnpm run typecheck:portable` 通过 |
| 全项目类型检查 | 生成不含环境变量值的 Worker 声明后，`tsc --noEmit` 通过 |
| 代码规范检查 | 0 errors；保留既有 50 warnings，不为消除警告修改冻结图片呈现或动效 |
| 本机浏览器 | 页面正常加载；P1 动画后入口可用，P1→P2→P3 和问事输入可操作；实际 390×844 CSS 视口无横向溢出 |

浏览器对其他目标尺寸的覆盖设置没有在读取的实际尺寸中得到确认，**不能算作 360/430 或跨机型验收通过**。工具在切换尺寸时出现缩小的截图表现，但 DOM 布局仍为 390 CSS 像素；没有据此修改冻结布局。

测试使用既有固定模型输出样本，不消耗真实模型额度；它验证的是迁移接口、持久层和传输，不是新公网部署的真实模型质量或可达性。本机临时服务已关闭，浏览器尺寸覆盖已重置。

## 部署阻塞证据

- 现有 Render 服务：`abalo-owner-preview-engine`，ID `srv-d9gbf261a83c73bo93lg`；其环境页面仍在用户 Chrome 标签中。
- Chrome 能列出该标签，但读取 DOM 返回 `Debugger unattached`；新建控制台标签也超时，重新取得标签未解决。
- 本地网页可由 Codex 内置浏览器正常访问；改用同一内置浏览器访问 `https://dashboard.render.com` 仍在 60 秒内超时。
- 当前可用工具没有 Render 连接器；环境中未发现可用部署平台凭据。没有从浏览器内部状态、配置文件或其他隐蔽位置提取密钥。
- 没有尝试通过关闭安全保护、修改系统代理或购买服务绕开此阻塞。

## 恢复后的执行顺序

1. 恢复对用户已登录 Render 控制台的有效浏览器连接；若需用户协助，先让用户确认该控制台能正常打开，并重新连接浏览器。
2. 检查独立部署目标与存储方案。Render 只是沿用现有账户的候选，不代表其域名已通过大陆直连。需要收费资源时先确认金额；不能静默购买。
3. 创建独立源码分支和新服务，保留原入口。不要推送替换 `main` 或修改旧引擎部署。
4. 安全配置已有引擎连接，核验真实完整生成、新 origin、代理身份、持久数据及失败路径。
5. 对实际新网址进行可用的公开网络检查；没有大陆节点证据时明确说明，不把境外/本机成功当成大陆成功。
6. 达到线上准入条件后再交产品负责人新网址，关闭 VPN 完成移动数据/Wi-Fi、系统浏览器/微信和不同尺寸的真机验收。

## 尚未完成/不得宣称

- 新服务部署、真实模型端到端、新网址大陆直连、全 P1–P9 跨机型视觉验收。
- 线上 D1 历史数据及旧域名设备身份迁移。更换域名不会自动带走原安装状态或设备存储。
- 多实例扩展、生产存储与运营保障。当前适配为单实例 SQLite。
- 代理链尚未核验时，每小时额度可能由代理后的多个用户共同使用；不能声称已经实现真实逐用户限流。

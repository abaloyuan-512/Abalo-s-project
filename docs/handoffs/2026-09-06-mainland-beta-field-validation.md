# 观象大陆直连 Beta 现场验证台账（2026-09-06）

## 当前结论

大陆直连 Beta 已确认两台大陆无 VPN 安卓手机可以打开，其中产品负责人已完成 P1–P9 全流程；两台 iPhone 的 Safari 均停在 P1。iPhone 截图显示 HTML/CSS 外壳已经到达，但 P1 主画面显示为破图，因此不是“整个网址无法连接”，而是首屏媒体兼容/恢复缺口。首位用户的详细解卦还曾发生一次可重试错误。第三阶段状态保持“公开 Beta 验证中”。

## 已知现场结果

| 编号 | 设备/网络 | 打开入口 | 完成 P1–P9 | 详细解卦首次成功 | 证据状态 |
|---|---|---:|---:|---:|---|
| T1 产品负责人 | 华为、无 VPN；网络类型待补 | 是 | 是 | 否；重试后成功 | 用户已确认 |
| T2 同事 | 小米、无 VPN；网络类型待补 | 是且流畅 | 待补 | 待补 | 用户已确认打开和使用流畅 |
| T3 同事 | iPhone、Safari、无 VPN；网络类型待补 | 网页外壳是，P1 主画面否 | 否 | 不适用 | 用户转述 |
| T4 同事 | iPhone、Safari、5G、无 VPN | 网页外壳是，P1 主画面否 | 否 | 不适用 | 有截图；发生时间约 00:03，日期待补 |
| T5 同事 | 待测试 | 待测试 | 待测试 | 待测试 | 待收集 |

不得记录真实姓名、手机号、IP、问题正文或解卦正文。只记录设备型号、系统、浏览器、网络类型、时间、阶段、错误文字与是否重试成功。

## 已完成的非大陆诊断

- `https://guanxiang-mobile-beta.onrender.com/healthz` 当前返回 HTTP 200；本机测得首次响应约 0.89 秒。
- 阿里公共 DNS（223.5.5.5）、腾讯公共 DNS（119.29.29.29）与 114 DNS 均能把域名解析到 `216.24.57.7`、`216.24.57.15`。
- 当前未取得该域名的 AAAA 地址。

这只能排除“当时全球服务完全宕机”和“三个常见大陆公共 DNS 均无法解析”两种情形，不能证明同事所在运营商到 Render/Cloudflare 边缘的实际链路可达。

## T2 必需证据

1. 无法打开时的完整截图，必须保留浏览器地址栏和错误文字。
2. 发生时间，精确到 5 分钟即可。
3. 手机型号、系统版本、浏览器名称。
4. 当时使用 Wi-Fi 还是移动数据；若为移动数据，记录运营商即可。
5. 等待了多久，刷新或切换一次网络后是否能打开。

## 失败分类

- 出现 Render `Application loading` 且约一分钟后进入：免费前端休眠唤醒，不是大陆封锁。
- `ERR_NAME_NOT_RESOLVED`、找不到服务器：DNS 路径问题。
- `ERR_CONNECTION_TIMED_OUT`、连接超时或一直白屏且服务日志无请求：运营商/跨境链路问题。
- 页面能打开，但成卦时提示“服务暂时繁忙/尚未提交”：后端唤醒或上游 429；不会重复成卦。
- 页面能打开，任务已经建立但解卦失败：区分传输异常与内容校验拦截，必须以错误文字和对应时间核对隐私安全日志。
- 页面呈现米色底和居中破图图标：HTML/CSS 已到达，P1 主图片没有成功解码或加载；不得归为 DNS 或整站不可达。

## iPhone P1 修复（已部署，待真机复测）

- P1 两张 PNG 原文件均为普通 8-bit sRGB，但各自额外带有约 25–29 KB 的 `caBX` 内容凭证块；安卓/桌面可解码，现场两个 Safari 样本均失败。当前只能把它列为最强兼容性嫌疑，不能在真机复测前宣称唯一根因。
- 新版 P1 使用新文件名和像素完全一致、已移除额外凭证块的 PNG，避免旧缓存继续命中原资源。
- 同时提供独立 JPEG 背景和可见 HTML 进入按钮；任一主图片失败时立即显示备用入口，视频未结束也会在 12 秒后自动解锁。
- 增加 `100vh` 和旧式媒体查询监听兼容，覆盖不支持 `svh` 或 `MediaQueryList.addEventListener` 的旧版 Safari。
- 桌面/安卓的正常 P1 视觉和 P1→P2 交互保持不变；正式结论等待 T3、T4 在部署后分别清除旧页面或使用新网址重测。
- 修复提交：`5d9863472b6136cb71c9de1fcb2f60ead29740cb`。
- Render 部署：`dep-daebugn74is73d700vg`，由产品负责人于 2026-09-06 09:18（GMT+8）在控制台手动触发。
- 2026-09-06 09:20 线上核验：主页 HTTP 200 并引用新 PNG；旧 PNG 文件名不再出现在主页；独立 JPEG 备用图返回 HTTP 200、`image/jpeg`、334254 字节；`/healthz` 返回 HTTP 200。

## 第三阶段冻结门槛

1. 至少 5 台由不同用户实际操作的大陆手机；同时覆盖 Wi-Fi 和移动数据，并尽量覆盖两个以上运营商或地区。
2. 所有测试者无 VPN 均能进入产品；允许免费服务首次唤醒，但 90 秒后仍打不开视为失败。
3. 至少完成 10 次 P1–P9 全流程；详细解卦首次成功率不低于 90%，且不存在连续两次仍无法恢复的失败。
4. 每类失败均有明确归因、用户提示和安全恢复路径；不得用自动重复提交模型请求掩盖失败。
5. 达标后记录固定提交、Render 部署、网址、测试样本数和已知免费托管限制，再冻结大陆直连 Beta 基线。

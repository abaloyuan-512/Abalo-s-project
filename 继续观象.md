# 继续观象项目

这是“观象”网站跨电脑、跨对话继续开发的固定入口。

## 给 Codex 的指令

当用户说“读取《继续观象.md》，接着昨天的任务继续”时：

1. 找到 GitHub 仓库 `abaloyuan-512/Abalo-s-project`，将本地代码同步到最新状态。
2. 使用分支 `codex/mvp-runnable-baseline`，不要从 `main` 开始修改。
3. 完整阅读仓库根目录的 `AGENTS.md`。
4. 完整阅读 `docs/handoffs/2026-07-19-guanxiang-visual-freeze.md`。
5. 查看最近提交和工作区状态，确认本机代码与远端分支一致。
6. 告诉用户你已经读取到的当前进度，然后从“逐页协商并定稿文字内容”阶段继续。

## 当前不可擅自改变的内容

- 视觉、排版、美术风格已经定稿并冻结。
- 除非用户明确提出，否则不要调整布局、配色、字体、太极八卦图、按钮样式或动效。
- 不得违反 `AGENTS.md` 中关于术数规则、确定性排盘、隐私资料和测试的约束。

## 当前下一步

从首页开始，一页一页与用户确认文字。先讨论、确认文字，再实施修改；不要一次性擅自改完整站文案。

以后每次跨设备交接，都优先更新本文件所指向的最新交接记录。这样用户只需要记住文件名“继续观象”，不需要记住 Git 命令、分支名或日期。

# 观象智能能力优化阶段1D

状态：`INVALID_RUN_CLOSE_COMPLETE`

唯一正式运行已完成，但全部请求在模型推理前被服务端以`invalid_json_schema`拒绝，未产生任何辨识语义证据。本阶段不得修补后重跑；详见`stage1d_acceptance_report.md`。

## 唯一验证命题

把独立就绪审查移到瓶颈提议之前，并用互斥结构合同及完整调用留痕约束输出，是否能稳定识别会改变卜题定义的关键缺口，同时避免对信息充分案例机械追问？

## 固定流程

```text
用户证据
→ 独立 Readiness Critic（看不到Proposer）
├─ VETO：程序只提出一个关键问题；不调用Proposer
└─ ALLOW：程序才调用Proposer，形成瓶颈假设并请求确认
```

第一次关键回答后若Critic仍然VETO，程序必须停止为`INSUFFICIENT_TO_CONFIRM`，不得提出第二问。任何模型、解析、合同或证据错误均失败关闭。

## 允许范围

- M01—M04核心开发验证集；
- 至少3个VETO与3个ALLOW的全新封存保护案例；
- Critic-first隔离合同、互斥证据值类型、仲裁器、runner、测试和治理记录；
- 保存每一次成功和失败调用的原始响应、解析结构、错误、模型、token、延迟和调用顺序；
- 唯一一次正式运行，模型重试为0。

## 禁止范围

- 不修改生产辨识、UI、API、数据库、路由或旧入口；
- 不排盘、不起卦、不解卦、不建设知识库或案例检索；
- 不增加模型角色或辨识字段；
- 不进入阶段2；
- 正式运行后不改Prompt、合同、验证器、runner或重跑。

## 通过标准

- M01—M04首次4/4正确VETO并命中已确认的关键维度；
- 加入冻结关键回答后4/4 ALLOW并生成证据落地的瓶颈；
- 新保护集首次分类6/6正确，VETO维度正确，READY不被过度追问；
- 无第二个辨识问题、无`REVIEW_ERROR`；
- 调用记录与实际路径一致，专项测试和完整回归通过，生产零变更。

任何单项失败都不能由其他局部成功抵消。

# 偏差记录

## DEV-001：全量 pytest 默认临时目录权限失败

第一次全量运行在系统默认 pytest 临时根发生统一 `WinError 5`；964项已通过、110项fixture未建立。未修改代码，改用可写外部临时目录后全量通过。

## DEV-002：仓库内 basetemp 不满足旧评测的“证据必须在仓库外”测试

第二次运行使用仓库内隔离目录，得到1054通过、20失败；失败均是旧评测正确拒绝仓库内证据路径。随后改用仓库外临时目录，全量通过。这不是产品回归。

## DEV-003：初版释放门测试不足

PMO和红队在首次全绿后仍复现正文错盘、相对日期、无关经典、必然结果、读心与填充壳漏放。唯一Canary因此保持未执行；完成最小修复并把反例固化后重新验收。

## DEV-004：Canary后发现执行文件未保存不可变字节快照

V001调用时service/test是未跟踪文件。manifest哈希能证明调用时身份，但Canary后离线修复改变了工作区文件，仓库无法仅靠Git重建调用时字节。保留原执行哈希不覆盖；当前文件另记为V002候选。V002必须在调用前保存不可变快照或形成可定位commit。

## DEV-005：失败关闭缺少被拒输出的安全诊断证据

失败响应没有泄露正文，隐私边界正确；但Canary runner也未将被拒原文写入受控私有证据，导致只能看到`本卦_MISMATCH`，不能判断模型真错或误拦。V002需在不进入用户响应/普通日志的前提下保存私有受控输出，或至少保存更细的结构化诊断。
